; --- FFmpeg Paths ---
; Uncomment the line for your OS and provide the full path to the ffmpeg binary.

; For Windows (e.g., "C:/ffmpeg/bin/ffmpeg.exe")
; FFMPEG_WINDOWS_PATH = ""

; For Linux (e.g., "/usr/bin/ffmpeg")
; FFMPEG_LINUX_PATH = "/usr/bin/ffmpeg"